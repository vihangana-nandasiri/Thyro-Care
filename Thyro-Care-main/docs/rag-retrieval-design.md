# RAG retrieval design

Default mode: `KNOWLEDGE_RETRIEVAL_MODE=lexical` — token-overlap scoring, approved chunks only, min score + top-K bounds. Vector retrieval is optional behind an interface and is not active unless configured and validated. No public-web retrieval. No patient-data retrieval.
