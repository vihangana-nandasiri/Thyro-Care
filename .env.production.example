# Production frontend build variables (Cloudflare / CI).
# Never commit a real .env.production file.
# Changing VITE_API_BASE_URL requires a new frontend build and deployment.

VITE_API_BASE_URL=https://thyro-t1.onrender.com/api/v1
VITE_APP_NAME=ThyroCare AI
VITE_APP_ENV=production
VITE_GOOGLE_CLIENT_ID=
